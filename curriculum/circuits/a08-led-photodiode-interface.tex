\documentclass[border=4pt]{standalone}
\usepackage[siunitx]{circuitikz}

\begin{document}
\begin{circuitikz}[european resistors]
  % Low-side transistor LED driver.
  \draw (-3.5,0) node[ground] {}
    to[V, l_={$V_S$}] (-3.5,4.5)
    -- (2.0,4.5)
    to[R, l={$R_{LED}$}, i>^=$I_F$] (2.0,2.8)
    to[leD, l={$D_{LED}$}] (2.0,2.0)
    -- (2.0,1.6);
  \node[npn, anchor=C] (Q1) at (2.0,1.6) {};
  \draw (Q1.E) -- ++(0,-1.45) node[ground] {};
  \draw (Q1.B) -- ++(-1.0,0)
    to[R, l_={$R_B$}] ++(-1.8,0)
    node[ocirc, label={[font=\small]below:{TPIN: $V_{DRV}$}}] {};
  \node[font=\small] at (1.6,-1.0) {(a) switched LED indicator};

  % Zero-bias photodiode transimpedance amplifier.
  \node[op amp] (U1) at (8.6,2.4) {};
  \draw (U1.+) -- ++(-0.7,0) coordinate (VREFP);
  \draw (U1.-) -- ++(-1.2,0) coordinate (SUM);
  \draw (SUM) to[pD, l_={$D_{PD}$}, i>^=$I_{ph}$, invert] ++(0,-2.0)
    coordinate (VREFD);
  \draw (VREFD) -- (VREFD -| VREFP) -- (VREFP);
  \draw (VREFD) -- ++(-1.0,0)
    node[ocirc, label={[font=\small]left:{$V_{REF}$}}] {};
  \draw (SUM) -| (6.2,4.8)
    to[R, l^={$R_F$}] (10.1,4.8)
    -- (10.1,2.4) -- (U1.out);
  \draw (SUM) -| (6.2,3.9)
    to[C, l_={$C_F$}] (8.0,3.9)
    -- (10.1,3.9)
    -- (10.1,2.4);
  \draw (U1.out) -- ++(1.0,0)
    node[ocirc, label={[font=\small]right:{TPO: $V_O$}}] {};
  \node[font=\small] at (8.2,-1.0)
    {(b) zero-bias photodiode readout};
\end{circuitikz}
\end{document}
